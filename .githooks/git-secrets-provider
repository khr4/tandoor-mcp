#!/bin/sh
set -eu

cat <<'PATTERNS'
AKIA[0-9A-Z]{16}
ASIA[0-9A-Z]{16}
AIza[0-9A-Za-z_-]{35}
gh[pousr]_[0-9A-Za-z_]{36,}
github_pat_[0-9A-Za-z_]{80,}
sk-[A-Za-z0-9_-]{20,}
xox[baprs]-[0-9A-Za-z-]{10,}
Authorization:[[:space:]]*Bearer[[:space:]]+[A-Za-z0-9._~+/=_-]{24,}
TANDOOR_(API_)?TOKEN[[:space:]]*[:=][[:space:]]*['"]?[^'"[:space:]]{12,}
TANDOOR_MCP_TOKEN[[:space:]]*[:=][[:space:]]*['"]?[^'"[:space:]]{12,}
"TANDOOR_(API_)?TOKEN"[[:space:]]*:[[:space:]]*"[^"]{12,}"
"TANDOOR_MCP_TOKEN"[[:space:]]*:[[:space:]]*"[^"]{12,}"
td[a-z]_[0-9A-Za-z]{8,}_[0-9A-Za-z]{4,}_[0-9A-Za-z]{4,}_[0-9A-Za-z]{4,}_[0-9A-Za-z]{8,}
-----BEGIN[[:space:]]+(RSA|DSA|EC|OPENSSH|PGP|PRIVATE)[[:space:]]+PRIVATE[[:space:]]+KEY-----
PATTERNS
